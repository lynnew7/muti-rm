```
Usage: muti-rm [OPTIONS] <PATH>

Arguments:
  <PATH>  要删除的目标路径

Options:
  -t, --threads <THREADS>  线程数 (默认为CPU核心数) [default: 6]
  -h, --help               Print help
  -V, --version            Print version
```